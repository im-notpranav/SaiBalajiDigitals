# Frontend
# The web app uses a same-origin /api path by default (proxied by Vite in dev,
# by Cloudflare Pages Function in production). No build-time variable is needed.
# Set VITE_API_URL only if you need to point the frontend at a different API origin.
# VITE_API_URL=http://localhost:3001/api
